{
  "name": "smoketape",
  "version": "0.1.0",
  "description": "Local-first YAML smoke tapes for CLI proof reports.",
  "type": "module",
  "main": "./dist/src/api.js",
  "exports": {
    ".": {
      "types": "./dist/src/api.d.ts",
      "import": "./dist/src/api.js"
    }
  },
  "files": [
    "dist",
    "docs",
    "fixtures",
    "reports",
    "scripts",
    "README.md",
    "SECURITY.md",
    "LICENSE",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "RELEASE_NOTES.md"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "smoke": "npm run build && bash scripts/smoke.sh",
    "package:smoke": "node scripts/package-smoke.mjs",
    "release:validate:test": "node --test tests/release-validation.test.mjs",
    "audit": "npm audit --audit-level=low",
    "release:check": "npm run audit && npm run check && npm test && npm run build && npm run smoke && npm run package:smoke && npm run release:validate:test"
  },
  "keywords": [
    "cli",
    "smoke-test",
    "yaml",
    "local-first",
    "testing"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.3",
    "js-yaml": "^5.2.3"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  },
  "types": "./dist/src/api.d.ts",
  "bin": {
    "smoketape": "./dist/src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/smoketape.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/smoketape/issues"
  },
  "homepage": "https://github.com/rogerchappel/smoketape#readme"
}
